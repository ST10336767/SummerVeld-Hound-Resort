<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <!-- Permissions for image functionality -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".SummerveldApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SummerveldHoundResort"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">
        
        <!-- Google Services Configuration -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        
        <!-- New Activities from EditEvent branch -->
        <activity
            android:name=".DogActivity"
            android:exported="true" />
        <activity
            android:name=".EditEventActivity"
            android:exported="true" />
        <activity
            android:name=".AddEventActivity"
            android:exported="true" />
        <activity
            android:name=".ManageEventsActivity"
            android:exported="true" />
        <activity
            android:name=".ManageDogsActivity"
            android:exported="true" />
        <activity
            android:name=".EditDogActivity"
            android:exported="true" />
        <activity
            android:name=".AdminActivity"
            android:exported="true" />
        <activity
            android:name=".ui.admin.CreateDogActivity"
            android:exported="true" /> <!-- <activity -->
        <!-- android:name=".MainActivity2" -->
        <!-- android:exported="false" -->
        <!-- android:theme="@style/Theme.SummerveldHoundResort" /> -->
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>